name: Next.js CI

on:
  pull_request:
    branches: [ main ]  # ← PR作成時にCI実行
  push:
    branches: [ main ]  # ← mainにマージされたときにもCI実行

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Run lint
        run: npm run lint

      - name: Build project
        run: npm run build

      - name: Run tests
        run: npm test  # テストがないなら `echo "No tests"` にしておく

        env:
  SUPABASE_URL: ${{ https://ailxtaycofymuyuvgklw.supabase.co }}
  SUPABASE_KEY: ${{ eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFpbHh0YXljb2Z5bXV5dXZna2x3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDY5OTE1MTQsImV4cCI6MjA2MjU2NzUxNH0.lkqy8uHpQCG_Myd_Ci82R5zQTbocF1Y3Qci_qlW327M }}
